{
  "name": "yue2-groove-pod",
  "version": "0.1.0",
  "private": true,
  "description": "Phone-friendly launcher that provisions a RunPod GPU pod running YUE2 // GROOVE and hands back the link",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "types": "wrangler types"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "wrangler": "^4.40.0"
  }
}
